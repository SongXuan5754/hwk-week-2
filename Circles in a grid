 void setup(){
  size(500,500);
  background(0);
  noStroke();
  fill(255);

}

void draw () {
 
  int i = 30;
while(i<width-15){
  int j = 30;
  while(j<height-15) {
    ellipse(i, j,15,15 );
    j+=20;
  }
  i+=20;
}
}
